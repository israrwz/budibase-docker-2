deploy:
  - okteto build -t okteto.dev/budibase:${OKTETO_GIT_COMMIT} hosting 
  - okteto build -t okteto.dev/envoy:${OKTETO_GIT_COMMIT} hosting

devs:
  - hosting/docker-compose.yaml 
  - hosting/envoy.yaml
